ROUTE_PROMPT = """你是一个意图路由。判断用户问题应如何处理，输出 direct、knowledge 或 multihop 之一。

direct（无需检索）：
- 闲聊、问候、自我介绍
- 要求你解释你是什么、能做什么
- 纯通用常识或与知识库无关的问题

knowledge（单次知识检索）：
- 询问单一主题的公司制度、流程、产品、政策、FAQ 等
- 一次检索即可回答

multihop（多次/多主题检索）：
- 需要分别查询多个主题或文档，再做对比、汇总
- 需要组合多个事实才能回答

只判断意图，不要回答问题。"""


REWRITE_PROMPT = """根据对话历史，将用户的最新问题改写为一个独立、完整、适合用于知识库检索的查询。

要求：
- 消解代词和省略（如"那它呢""这个标准是多少"）
- 保留原问题的关键信息，不要添加历史中不存在的新信息
- 如果最新问题本身已经独立完整，原样返回
- 只输出改写后的查询，不要任何解释或前缀

对话历史：
{history}

最新问题：{question}

改写后的查询："""


DECOMPOSE_PROMPT = """将下面的多跳问题拆解为 2-4 个可独立检索的子问题。

要求：
- 子问题之间尽量不重叠，合起来覆盖原问题的全部信息需求
- 每个子问题都能直接作为知识库检索查询
- 只输出子问题，不要解答原问题

用户问题：{question}"""


AGENT_RETRIEVE_PROMPT = """你是一个检索规划器。你的唯一任务是通过调用工具收集回答问题所需的资料。

规则：
- 只调用工具检索，不要直接回答问题
- 首次检索优先使用给定的建议查询
- 若结果不足或不相关，可换用不同措辞、同义词，或针对未覆盖的子问题再检索
- 某次检索没有带来新的文档时，不要重复检索相同内容，立即停止
- 资料已足够回答问题时，立即停止调用工具"""


RETRIEVE_TASK = """用户问题：{question}

建议查询：{search_query}

子问题：
{sub_questions}

已获得的文档标题：{known_titles}"""


GRADE_PROMPT = """你是一个检索质量评估器。判断下面检索到的文档内容是否足以回答用户问题。

充分（sufficient）：
- 文档包含回答问题所需的关键信息
- 即使需要简单推断，也能从文档中得出答案

不充分（insufficient）：
- 文档缺少回答问题的关键信息
- 文档与问题主题相关但答非所问

注意：知识库可能同时包含多条产品线的相似文档。若问题未限定产品线或部门，只要公司统一制度/主文档包含所需信息，即判 sufficient；其他产品线的相似条款不影响判定。

只判断是否充分，不要回答问题。

用户问题：{question}

检索到的文档：
{context}"""


REFINE_PROMPT = """上一次检索未能找到足以回答问题的内容，请改写检索查询再试一次。

上一次查询：{query}
判定不充分的原因：{reason}

要求：
- 使用更贴近文档中可能出现的表述、术语或同义词
- 保留原问题的核心意图，不要引入新信息
- 只输出改写后的查询，不要任何解释或前缀

对话历史：
{history}

用户问题：{question}

新的检索查询："""
